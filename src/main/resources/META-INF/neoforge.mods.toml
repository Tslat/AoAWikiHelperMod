modLoader="javafml"
loaderVersion="${loader_version_range}"
license="MIT"

[[mods]]
modId="${mod_id}"
version="${wikihelper_version}"
displayName="AoA WikiHelper Mod"
credits="Silencer, for the everlasting wiki support for AoA. Fujino and Sonicwave for contributions to this mod and its continuation."
authors="Tslat"
description='''AoA WikiHelper Mod is a utility mod designed to interface with AoA and provide wiki-relevant info or pre-formatted data for use on the official wiki:
https://adventofascension.gamepedia.com'''

[[mixins]]
config="${mod_id}.mixins.json"

[[dependencies.${mod_id}]]
    modId="neoforge"
    type="required"
    versionRange="${neoforge_version_range}"
    ordering="NONE"
    side="BOTH"
[[dependencies.${mod_id}]]
    modId="minecraft"
    type="required"
    versionRange="${mc_version_range}"
    ordering="NONE"
    side="BOTH"
[[dependencies.${mod_id}]]
    modId="aoa3"
    type="required"
    versionRange="${aoa_version_range}"
    ordering="NONE"
    side="BOTH"